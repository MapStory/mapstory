/**
 * Automated Survey tests
 *
 * These tests are mapped to the survey tests here: https://rey52.typeform.com/to/IpjcXD
 */


describe('1. Tester Info', function() {
	it('> a) should have a username', function() {

	});

	it('> b) should have an email', function() {

	});

	it('> c) should have a test timestamp', function() {

	});

	it('> d) should specify a browser and size!', function() {

	});
});


describe('2. User  Profile', function() {
	it('> a) should create an account', function() {

	});

	it('> b) should receive confirmation email', function() {

	});

	it('> c) should confirm email', function() {

	});

	it('> d) should receive a welcome email', function() {

	});

	it('> e) should update profile info', function() {

	});
});

describe('3. Uploading  Datasets', function() {
	it('> a) should choose a dataformat', function() {
		/**
		 * CSV, SHP, KML, JSON
		 */
	});

	it('> b) should choose geometry', function() {
		// Points, Lines, Polygons
	});

	it('> c) should choose timescale', function() {
		// Billions, millions, current
	});

	it('> d) should upload dataset', function() {

	});

	it('> e) should update metadata', function() {

	});

	it('> f) should find published layers', function() {

	});

	it('> f) should download all filetypes', function() {

	});
});
